# There is no build step and no runtime dependency to install. Nothing should
# ever be generated inside the checkout: runs, logs, trust and snapshots live in
# the herdr plugin state directory, and the tests write to temp directories.
# These entries keep editor, OS and npm noise out.
.DS_Store
*.log
node_modules/
coverage/
*.tgz
.idea/
.vscode/
.claude/settings.local.json
